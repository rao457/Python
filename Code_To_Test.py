def name(first,last):
    """ This function takes user first and last name and format them neatly."""
    formateed_name = f"{first} {last}"
    return formateed_name.title()
def cities(city, country):
    """ This function takes city and coutry name and return formateed name"""
    formatted_city = f"{city}, {country}"
    return formatted_city.title()
